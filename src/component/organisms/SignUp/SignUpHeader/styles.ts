import styled from "styled-components";

export const SignUpHeaderLayout = styled.header`
  margin: 0;
  margin-top: 22px;
  display: flex;
  align-items: center;
  margin-right: 18px;

  & button {
    margin: 0;
    position: absolute;
  }

  & span {
    margin: 0px auto;
  }
`;
