import styled from "styled-components";

export const DefaultLayout = styled.div`
  width: 100%;
  bottom: 0;
  position: fixed;

  & > button {
    position: fixed;
    right: 44px;
    bottom: 123px;
  }
`;
